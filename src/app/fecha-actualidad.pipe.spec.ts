import { FechaActualidadPipe } from './fecha-actualidad.pipe';

describe('FechaActualidadPipe', () => {
  it('create an instance', () => {
    const pipe = new FechaActualidadPipe();
    expect(pipe).toBeTruthy();
  });
});
